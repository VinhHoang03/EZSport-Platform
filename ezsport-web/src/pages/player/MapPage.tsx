// MapPage renders the full player map + court list experience (App.tsx)
import React from 'react';
import App from '../../App';

const MapPage: React.FC = () => <App />;

export default MapPage;