// Wrapper page — full map + court list view
// MapPage is handled inside App.tsx for now (complex state)
// This is a placeholder until the map logic is extracted
import React from 'react';

const MapPage: React.FC = () => (
  <div className="min-vh-100 d-flex align-items-center justify-content-center">
    <p className="text-muted">Map page — coming soon</p>
  </div>
);

export default MapPage;